<Window x:Class="LibraryTrackerApp.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryTrackerApp.View"
        mc:Ignorable="d"
        Title="LoginView" Height="500" Width="500"
        Background="#F3F4F6"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button" x:Key="LoginButton">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>

            <Style.Triggers >
                <Trigger Property="IsMouseOver" Value="True" >
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>

            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        
        <Border CornerRadius="20" BorderThickness="1"
                BorderBrush=  "#E5E7EB"
                Margin="20"
                Padding="30"
                Width="350"
                Background="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                >
            <Border.Effect>
                <DropShadowEffect
                    Color="#000000"
                    BlurRadius="20"
                    ShadowDepth="3"
                    Opacity="0.25"
                 />

            </Border.Effect>
            <Grid >
                <StackPanel VerticalAlignment="Top">
                    <!-- Title-->
                    <TextBlock 
                        Text="Welcome to Login Page !"
                        FontSize="22"
                        HorizontalAlignment="Center"
                        FontWeight="Bold"
                        Margin="0,0,0,30"
                        
                        />
                    <!-- Username-->
                    <TextBlock 
                        Text="Username"
                        FontSize="14"
                       
                        FontWeight="SemiBold"
                    
                        />
                    <TextBox 
                        Margin="0,5,0,20"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Height="35"
                        Padding="8"
                        
                        />

                    <!-- Password-->
                    <TextBlock 
                        Text="Password"
                        FontSize="14"                      
                        FontWeight="SemiBold"                     
                        />

                    <PasswordBox                 
                     FontSize="14"
                     FontWeight="SemiBold"
                     Margin="0,5,0,20"   
                        Padding="8"                        
                     />
                    <!-- Login Button-->

                    <Button 
                        Content="Login"  
                        Margin="5,10,5,20"
                       Style="{StaticResource LoginButton}"
                      
                        >

                    </Button>
                </StackPanel>

            </Grid>

        </Border>

    </Grid>
    
</Window>
