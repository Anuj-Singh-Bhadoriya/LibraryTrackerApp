<Window x:Class="LibraryTrackerApp.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library Tracker" Height="500" Width="900">

    <Window.Resources>

        <!-- Button Style -->
        <Style TargetType="Button" x:Key="ButtonTypeStyle">

            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="35"/>

            <Setter Property="Template">
                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border x:Name="border"
                                CornerRadius="8"
                                BorderThickness="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- HoverEffect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="#2563EB"/>

                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#E5E7EB"/>
                            </Trigger>

                            <!-- Click -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                        Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>
            </Setter>

        </Style>

    </Window.Resources>


    <Grid>

        <!-- Layout Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- HEADER -->
        <Border Grid.Row="0"
                Margin="5"
                CornerRadius="8"
                BorderBrush="#E5E7EB"
                BorderThickness="1">

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2563EB" Offset="0"/>
                    <GradientStop Color="#60A5FA" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Border.Effect>
                <DropShadowEffect
                    Color="#000000"
                    BlurRadius="20"
                    ShadowDepth="3"
                    Opacity="0.25"/>
            </Border.Effect>

            <TextBlock
                Text="Welcome to the Library Tracker"
                FontSize="18"
                FontWeight="Bold"
                Foreground="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>

        </Border>


        <!-- MAIN CONTENT -->
        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <!-- SIDEBAR -->
            <Border Grid.Column="0"
                    Background="#2c3e50">

                <StackPanel>

                    <TextBlock
                        Text="Library Menu"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="White"
                        Margin="10"
                        HorizontalAlignment="Center"/>

                    <Button
                        Content="Dashboard"
                        Style="{StaticResource ButtonTypeStyle}"
                        Click="DashboardButton_Click"
                        />

                    <Button Content="Books"
                            Style="{StaticResource ButtonTypeStyle}"
                            Click="BooksButton_Click"
                            />

                    <Button Content="Members"
                            Style="{StaticResource ButtonTypeStyle}"
                            Click="MemberButton_Click"
                            />

                    <Button Content="Issue Book"
                            Style="{StaticResource ButtonTypeStyle}"/>

                    <Button Content="Return Book"
                            Style="{StaticResource ButtonTypeStyle}"/>

                </StackPanel>

            </Border>


            <!-- CONTENT AREAc:\project_vs2026\wpf_project\librarytrackerapp\librarytrackerapp\view\mainwindowview.xaml
c:\project_vs2026\wpf_project\librarytrackerapp\librarytrackerapp\view\mainwindowview.xaml.cs
 -->
            <Grid Grid.Column="1" Background="White">

                <ContentControl x:Name="MaincontentControl" />
                

            </Grid>

        </Grid>

    </Grid>

</Window>