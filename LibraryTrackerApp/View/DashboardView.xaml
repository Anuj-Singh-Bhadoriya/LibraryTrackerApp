<UserControl x:Class="LibraryTrackerApp.View.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryTrackerApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="800">
    <Grid Background="White" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
        Text="Dashboard"
        FontSize="24"
        FontWeight="Bold"
        Margin="10"/>

        <!-- Stats Cards -->
        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Total Books -->
            <Border Margin="10" Padding="20" CornerRadius="8" Background="White">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Total Books" FontSize="14"/>
                    <TextBlock Text="120" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Total Members -->
            <Border Grid.Column="1" Margin="10" Padding="20" CornerRadius="8" Background="White">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Total Members"/>
                    <TextBlock Text="35" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Issued -->
            <Border Grid.Column="2" Margin="10" Padding="20" CornerRadius="8" Background="White">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Issued Books"/>
                    <TextBlock Text="20" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Available -->
            <Border Grid.Column="3" Margin="10" Padding="20" CornerRadius="8" Background="White">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Available"/>
                    <TextBlock Text="100" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </Border>

        </Grid>

    </Grid>
</UserControl>
