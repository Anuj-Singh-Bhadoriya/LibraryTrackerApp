<UserControl x:Class="LibraryTrackerApp.View.IssueBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryTrackerApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.Row="1" Margin="20" Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <!-- Member ID -->
        <TextBlock Text="Member Id" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Member Name -->
        <TextBlock Text="Member Name" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Book ID -->
        <TextBlock Text="Book Id" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Book Name -->
        <TextBlock Text="Book Name" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5"/>

        <!-- Issue Date -->
        <TextBlock Text="Issue Date" Grid.Row="4" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="4" Grid.Column="1" Margin="5"/>

        <!-- Due Date -->
        <TextBlock Text="Due Date" Grid.Row="5" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="5" Grid.Column="1" Margin="5"/>

        <!-- Button -->
        <Button
        Grid.Row="6"
        Grid.Column="1"
        Content="Issue Book"
        Width="120"
        Height="35"
        Margin="5"/>

    </Grid>
</UserControl>
