<UserControl x:Class="LibraryTrackerApp.View.BookView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Height="400" Width="650">

    <Grid Background="White" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock
Text="Books Management"
FontSize="20"
FontWeight="Bold"
VerticalAlignment="Center"
   HorizontalAlignment="Center"     
            />

        <!-- Search -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">

            <TextBlock Text="Search Book:"
VerticalAlignment="Center"
Margin="0,0,10,0"/>

            <TextBox Width="200"
                     Margin="5"
                     />

            <Button Content="Search"
Margin="5"
Width="80"/>

        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5">

            <Button Content="Add Book"
Width="100"
Margin="5"/>

            <Button Content="Update Book"
Width="100"
Margin="5"/>

            <Button Content="Delete Book"
Width="100"
Margin="5"/>

        </StackPanel>

        <!-- Books Table -->
        <DataGrid Grid.Row="3"
AutoGenerateColumns="False"
Margin="5">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ID" Width="50"/>

                <DataGridTextColumn Header="Title" Width="*"/>

                <DataGridTextColumn Header="Author" Width="*"/>

                <DataGridTextColumn Header="Category" Width="*"/>

                <DataGridTextColumn Header="Year" Width="80"/>

                <DataGridTextColumn Header="Status" Width="100"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>
